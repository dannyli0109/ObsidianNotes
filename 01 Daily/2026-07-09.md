# 2026-07-09

## 今日目标
- [x] 学习一个小时godot

## 今天学了什么
- [脚本语言](https://docs.godotengine.org/zh-cn/4.x/getting_started/step_by_step/scripting_languages.html)
## 今天写了什么
- Hello World
```GDScript
  func _init():
	print("Hello, world")
```
- 旋转脚本
```GDScript
extends Sprite2D

var speed = 400
var angular_speed = PI
var velocity = Vector2.ZERO
func _process(delta: float):
	var direction = 0
	if Input.is_action_pressed("ui_left"):
		direction = -1
	if Input.is_action_pressed("ui_right"):
		direction = 1
	rotation += angular_speed * direction * delta
	if Input.is_action_pressed("ui_up"):
		velocity = Vector2.UP.rotated(rotation) * speed
	position += velocity * delta
```
- 信号
```GDScript
extends Sprite2D

var speed = 400
var angular_speed = PI

func _ready() -> void:
	var timer = get_node("Timer")
	timer.timeout.connect(_on_timer_timeout)

func _process(delta: float):
	rotation += angular_speed * delta
	var velocity = Vector2.UP.rotated(rotation) * speed
	position += velocity * delta


func _on_button_pressed():
	set_process(not is_processing())
	
func _on_timer_timeout():
	visible = not visible
```
- 自定义信号
```GDScript
extends Node2D

signal health_depleted
var health = 10
# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	take_damage(1)
	
func take_damage(amount):
	health -= amount
	if health <= 0:
		health_depleted.emit()
```
![[Pasted image 20260710010722.png]]
直接连接就好了
## 卡住的问题
- 改了速度没有改位置
- 自定义事件需要自己连线

## Bug / Fix
- 

## 明天继续
- 学[第一个2d游戏](https://docs.godotengine.org/zh-cn/4.x/getting_started/first_2d_game/)